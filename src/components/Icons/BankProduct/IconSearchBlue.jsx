import React from 'react';
import iconSearch from '../../../assets/icons/Bank_Product/Search.png';

const IconSearchBlue = ({ className }) => {
  return <img className={className} src={iconSearch} alt='Icon Search Blue' />;
};

export default IconSearchBlue;
